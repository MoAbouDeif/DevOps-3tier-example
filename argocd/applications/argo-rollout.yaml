apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argo-rollouts
spec:
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  source:
    path: ''
    repoURL: https://argoproj.github.io/argo-helm
    targetRevision: 2.40.3
    chart: argo-rollouts
    helm:
      parameters:
        - name: installCRDs
          value: 'false'
      values: |
        dashboard:
          enabled: true
          ingress:
            enabled: true
            ingressClassName: alb
            hosts: ["rollouts.deifops.click"]


  sources: []
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      enabled: true
    syncOptions:
      - CreateNamespace=true
